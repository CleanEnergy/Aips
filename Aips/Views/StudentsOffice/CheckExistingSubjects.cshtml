@model IEnumerable<EntityClasses.SubjectViewModel>

@{
    ViewBag.Title = "CheckExistingSubjects";
}

<h2>All subjects</h2>

<p>
    @Html.ActionLink("Create New", "CreateSubject")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsActive)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Year)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Degree)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsActive)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Year)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Degree)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditSubject", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "SubjectDetails", new { /* id=item.PrimaryKey */ }) |
        </td>
    </tr>
}

</table>
